# Use GCC image for C++ dev
FROM gcc:latest

# Install any build tools you want (optional)
RUN apt-get update && apt-get install -y cmake make

# Set working directory
WORKDIR /workspace

# Default command
CMD [ "bash" ]
